{
  "name": "node-react-fullstack",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"shared,backend,frontend\" --prefix-colors \"yellow,cyan,magenta\" \"npm run dev -w packages/shared\" \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/backend && npm run build -w packages/frontend"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "concurrently": "^9.1.2"
  }
}
